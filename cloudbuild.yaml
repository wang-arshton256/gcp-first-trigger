steps:
  - name: 'alpine'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
          echo "Cloud Build Triggered Successfully!"
          echo "Repository: $REPO_NAME"
          echo "Branch: $BRANCH_NAME"
          echo "Commit ID: $COMMIT_SHA"

#options:
  #logging: CLOUD_LOGGING_ONLY
  logging: NONE

logsBucket: gs://gcp-devops-course-bucket-new